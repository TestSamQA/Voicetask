<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <!-- Clear Windows/Visual Studio fallback package folders so Docker builds
       on Linux don't fail looking for paths that don't exist in the container. -->
  <fallbackPackageFolders>
    <clear />
  </fallbackPackageFolders>
</configuration>
